# rrq-protocol

Shared RRQ stdio protocol types for Rust executors.

## Git dependency

```toml
[dependencies]
rrq-protocol = { git = "https://github.com/YOUR_ORG/rrq", package = "rrq-protocol", rev = "<sha>" }
```
