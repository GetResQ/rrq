# Python Reference Orchestrator

This directory contains a Python orchestrator implementation used for
conformance tests and behavior reference. It is not part of the primary runtime
path.

Primary runtime code lives in:
- Rust orchestrator: `rrq-rs/rrq-orchestrator`
- Python producer/executor: `rrq/`
